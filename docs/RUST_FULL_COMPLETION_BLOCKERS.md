# Rust Full Completion Blockers

No open blockers remain for Rust-only `main`.

Rust v3 is the only supported product path in current `main`. The previous
Python/FastAPI v2 compatibility implementation was removed from `main` and is
preserved only in git history at tag `pre-rust-only-legacy-v2`.

Non-blocking future enhancements:

- production embedding provider integrations
- published npm/Homebrew channels
- signed release artifacts
- richer MCP compatibility matrix
